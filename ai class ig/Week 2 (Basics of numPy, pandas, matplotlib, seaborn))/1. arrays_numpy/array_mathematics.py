import numpy as np

# add arrays
a = np.array([1, 2, 3])
b = np.array([4, 5, 6])

ab = a+b # = 5, 7, 9
axb = a*b # = 4, 10, 18
adivb = a/b # = 1/4, 2/5, 3/6

arr = np.array([1, 2, 3])

# get sqrt of array of each element
np.sqrt(arr)

# get sum of array
np.sum(arr) # = sum of all nums in array

np.mean(arr) # mean of all nums in array

np.max(arr) # get largest element