from functools import reduce
import os
import sys

# printing the file name as well as python version
# print(sys.argv)
# print(sys.version)

# print(os.getcwd())
# os.mkdir("test_dir")


# in line function?
# add = lambda x, y: x + y
# print(add(1, 2))

# numbers = [1, 2, 3, 4]

# map
# maps/replaces numbers in numbers by squaring them
# squares = map(lambda x: x**2, numbers)
# print(list(squares))

# filter
# filters/removes uneven nymbers from numbers
# evenList = filter(lambda x: x % 2 == 0 , numbers)
# print(list(evenList))

# reduce
# not exactly 1005 sure but this kinda multiplies all of the numbers together 
# product = reduce(lambda x, y: x * y, numbers)
# print(product)

